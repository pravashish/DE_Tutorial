version: "3.9"

services:
  learning-hub:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./pages:/app/pages
    environment:
      - STREAMLIT_SERVER_RUN_ON_SAVE=true
    restart: unless-stopped
